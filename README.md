# BrowserPicker.Actions

Reusable GitHub Actions workflows for BrowserPicker.

This repository exists to keep the build and release workflow definitions outside
the main BrowserPicker repository. BrowserPicker only keeps small caller
workflows locally and pins them to a specific commit from this repository.
